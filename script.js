document.addEventListener('DOMContentLoaded', function() {    const menuToggle = document.getElementById('menuToggle');    const nav = document.querySelector('nav');    const backButton = document.querySelector('.back-button');        // Function to open the menu    function openMenu() {        nav.classList.add('open');        document.querySelector('main').classList.add('menu-open');    }    // Function to close the menu    function closeMenu() {        nav.classList.remove('open');        document.querySelector('main').classList.remove('menu-open');    }    // Event listener for the menu toggle button    menuToggle.addEventListener('click', function() {        openMenu();    });    // Event listener for the back button in the menu    backButton.addEventListener('click', function() {        closeMenu();    });    // Function to replace text dynamically    function replaceText() {        const textElements = document.querySelectorAll('*');        textElements.forEach(element => {            if (element.hasChildNodes()) {                element.childNodes.forEach(node => {                    if (node.nodeType === Node.TEXT_NODE) {                        node.textContent = node.textContent                            .replace(/Munavvar/g, 'മുനവ്വർ')                            .replace(/Mukhaddas/g, 'മുഖദ്ദസ്')                            .replace(/Mukarram/g, 'മുഖറം');                    }                });            }        });    }    // Call the function to replace text on page load    replaceText();});